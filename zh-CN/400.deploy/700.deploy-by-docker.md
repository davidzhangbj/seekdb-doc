# 部署 SeekDB 容器环境

本文介绍如何通过 Docker 容器部署 SeekDB。

## 前提条件

在执行本文操作前，您需确认以下信息：

* 您已安装 Docker 并启动 Docker 服务，详细操作请参考 [Docker 文档](https://docs.docker.com/get-docker/)。
  
  <main id="notice" type='explain'>
    <h4>说明</h4>
    <p>在使用 x86 架构的 MAC 机器中，仅支持使用 V4.9.0 及以下版本的 Docker 部署 SeekDB，可点击 <a href="https://desktop.docker.com/mac/main/amd64/81317/Docker.dmg?_gl=17jelfd_gcl_auOTk5Nzk0MDUwLjE3MTE4ODMyNzM._gaNDQyMjE1MDE5LjE3MTE4ODMyNzQ._ga_XJWPQMJYHQ*MTcxOTIxOTEwMy4xMS4xLjE3MTkyMjEwMTAuNjAuMC4w">链接</a> 下载 Docker。</p>
  </main>

* 您的机器满足软硬件要求。详细信息，参考 [服务器配置要求](300.preparations-before-deploy/100.prepare-servers.md)。

## 部署 SeekDB

### （可选）步骤一：拉取 SeekDB 镜像

运行如下命令，拉取 SeekDB 所需镜像。

* 搜索 SeekDB 相关镜像

  ```shell
  [admin@test001 ~]$ sudo docker search oceanbase-seekdb
  ```

* 拉取 SeekDB 最新镜像

  ```shell
  [admin@test001 ~]$ sudo docker pull oceanbase/oceanbase-seekdb
  ```

  <main id="notice" type='explain'>
    <h4>说明</h4>
    <ul>
    <li>
    <p>若拉取 Docker 镜像失败，您也可从 quay.io 或者 ghcr.io 仓库中拉取镜像，只需将上述拉取命令中的 <code>oceanbase/oceanbase-seekdb</code> 对应替换为 <code>quay.io/oceanbase/oceanbase-seekdb</code> 或 <code>ghcr.io/oceanbase/oceanbase-seekdb</code>，如执行 <code>sudo docker pull quay.io/oceanbase/oceanbase-seekdb</code> 从 quay.io 中拉取镜像。</p>
    </li>
    <li>
    <p>本步骤命令中的仓库地址更换后，步骤二中的命令需同步替换仓库地址，两条命令所用仓库需保持统一。</p>
    </li>
    <li>
    <p>上述命令默认拉取最新版本，可根据实际需求在 <a href="https://hub.docker.com/r/oceanbase/oceanbase-ce/tags">dockerhub</a>、<a href="https://quay.io/repository/oceanbase/oceanbase-ce?tab=tags">quay.io</a> 或 <a href="https://github.com/oceanbase/docker-images/pkgs/container/oceanbase-ce">ghcr.io</a>中选择版本。</p>
    </li>
    </ul>
  </main>

### 步骤二：启动 SeekDB 实例

运行如下命令，启动 SeekDB 实例。

```shell
[admin@test001 ~]$ sudo docker run -d --restart unless-stopped -p 2881:2881 -v /home/admin/seekdb:/var/lib/oceanbase  oceanbase/oceanbase-seekdb
```
示例中的选项作用如下：

* `-p` 用于将容器的端口映射到宿主机端口，示例中是将容器中的 `2881` 端口分别映射到宿主机的 `2881` 端口。

* `-v` 用于在容器与宿主机之间共享文件或目录，实现数据持久化或配置共享。默认情况下，系统会在容器的 `/root/ob` 目录下部署 OceanBase 数据库，并在 `/root/.obd/cluster` 下保存其配置。您可使用该选项在宿主机上持久化数据。
  
  您也可使用 `-v` 选项挂载需要执行的 SQL 去执行，示例如下，您需将 `{init_sql_folder_path}` 修改为实际初始化 SQL 的文件路径。

  ```shell
  [admin@test001 ~]$ sudo docker run -p 2881:2881 -p 2886:2886 -v {init_sql_folder_path}:/root/boot/init.d --name obstandalone -e MODE=SLIM -d oceanbase/oceanbase-ce
  ```

* `--name` 用于设置 Docker 容器名称，比如示例中创建一个名为 `obstandalone` 的 Docker 容器。


### 步骤三：连接 OceanBase 数据库实例

您可选择进入容器，使用 MySQL 客户端 或 OBClient 客户端连接实例，也可使用宿主机本地 OBClient 或 MySQL 客户端连接到 SeekDB 实例。

#### 进入容器后连接

1. 进入 Docker 容器

   ```shell
   [admin@test001 ~]$ sudo docker exec -it obstandalone bash
   ```

2. 连接集群

   ```shell
   mysql -h127.0.0.1 -uroot -A -Doceanbase -P2881 -p
   ```

#### 使用宿主机本地客户端连接

您可选择使用宿主机本地 OBClient 或 MySQL 客户端连接到 SeekDB 实例，示例如下：

```shell
[admin@test001 ~]$ mysql -uroot -h127.0.0.1 -P2881 -p
```

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>若启动 SeekDB 实例时未通过环境变量配置密码，实例中创建的用户默认使用空密码。</p>
</main>

连接成功后，终端将显示如下内容：

```shell
Welcome to the MariaDB monitor.  Commands end with ; or \g.
Your MySQL connection id is 3221493186
Server version: 5.7.25 OceanBase SeekDB 1.0.0.0 (r42025102311-1a2528277697a22df5884a58064cdc2b4117c07c) (Built Oct 23 2025 12:06:09)

Copyright (c) 2000, 2018, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MySQL [(none)]>
```
