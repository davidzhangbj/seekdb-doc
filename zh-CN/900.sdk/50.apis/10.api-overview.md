---
slug: /api-overview
---

# API 参考

seekdb 支持通过 API 以编程的方式来使用 seekdb。

## API 接口

目前支持以下 API 接口。

### Database

:::info
仅支持在使用 `AdminClient` 连接时，才能使用该接口。关于 `AdminClient` 的详细介绍，参见 [Admin Client](../100.admin-client.md)。
:::

|API 接口|描述|
|---|---|---|
|[xx]()|XXXX| |

### Collection

:::info
仅支持在使用 `Client` 连接时，才能使用该接口。关于 `Client` 的详细介绍，参见 [Client](../50.client.md)。
:::

|API 接口|描述|
|---|---|---|
|[xx]()|XXXX| |

### DML

:::info
仅支持在使用 `Client` 连接时，才能使用该接口。关于 `Client` 的详细介绍，参见 [Client](../50.client.md)。
:::

|API 接口|描述|
|---|---|---|
|[xx]()|XXXX| |

### DQL

:::info
仅支持在使用 `Client` 连接时，才能使用该接口。关于 `Client` 的详细介绍，参见 [Client](../50.client.md)。
:::

|API 接口|描述|
|---|---|---|
|[xx]()|XXXX| |