# SeekDB 概述

OceanBase SeekDB（简称 SeekDB）是 OceanBase 推出的一款全新的 AI-Native 轻量级数据库产品，支持基于向量、全文、标量混合计算的语义及关键词混合搜索。作为 OceanBase 的新一代引擎，SeekDB 融合了核心引擎的高性能与 MySQL 全面兼容能力，秉持 AI-First 理念，致力于为 AI 时代应用提供坚实且智能的数据底座。

## 产品架构



## 核心特性

### 极简部署，秒级初始化

单点架构，无需复杂配置，秒级即可完成初始化。支持 Client/Server 和嵌入式两种部署模式。Client/Server 部署模式下，支持 yum install、docker 或 windows/macos 桌面版部署方式；嵌入式部署模式下，目前仅支持 python 嵌入。

### 最低 1C1G 小规格，垂直弹性扩缩容

最低 1 核 CPU + 1GB 内存即可启动试用，向量搜索、混合搜索试用场景，建议内存不低于 4GB。对系统并发量、数据量、查询复杂度有较高要求时，可灵活垂直扩展资源规格。

### 高性能向量索引、全文索引，向量、全文、标量混合搜索

SeekDB 支持高达 16,000 维向量存储与高性能检索，兼容 L2、内积、余弦相似度等多种距离计算方式，提供 HNSW/IVF 系列索引及相关量化算法，支持近似最近邻搜索。

除了语义搜索，SeekDB 还支持了高性能全文索引，基于 BM25 相关性排序算法，实现关键词精准搜索。支持 Space、Beng、Ngram、IK、Jieba 等多种分词器，提供 Natural Language Mode、Boolean Mode、Phrase Query、Multi Match 等多种查询模式，可在海量文本数据中快速定位目标信息，提升查询效率。
更值得关注的是，SeekDB 支持了向量、全文、标量、空间等多类数据的混合搜索，一条 SQL 完成多路查询与重排序。同时 SeekDB 还提供了 Hybrid Vector Index 功能，只需写入文本数据，系统自动进行 Embedding 生成向量索引，查询时也仅需指定文本搜索条件，即可进行语义搜索。满足 RAG 技术方案下对多模数据混合查询的需求，助力企业构建高效智能的数据检索系统。

支持 SQL、Python SDK、Java SDK 多种访问接口，适用于各种应用开发环境。

### 无缝对接各类模型，内置 AI Function 实现库内实时推理

SeekDB 支持 Chat Model、Embedding Model 接入，提供 DBMS_AI_SERVICE 系统包，来管理 AI 模型信息。内置 AI_COMPLETE、AI_EMBED、AI_RERANK 等 AI Function，基于 SQL 表达式即可在数据库内进行 AI 数据处理和实时推理。

### 基于 JSON 的动态 Schema，支持文档元数据动态存储和高效访问。

SeekDB 支持 JSON 数据类型，具备动态 Schema 能力。支持 JSON 部分更新降低数据更新成本，并提供 JSON 函数索引、多值索引优化查询性能。实现半结构化编码，降低存储成本。在 AI 应用中，JSON 可作为文档元信息的存储类型，并支持和全文、向量的混合搜索能力。

### 数据实时写入，实时可查

基于 LSM-Tree 存储架构，SeekDB 支持数据的高频实时写入。数据 DML 时同步构建各类索引，入库成功立即可查。

### 兼容 MySQL 不止于 MySQL，支撑 HTAP 混合负载 

高度兼容 MySQL 的语法、协议、视图等，但比 MySQL 提供了面向更广阔场景的计算和存储引擎的支持。基于行列混存技术、向量化执行能力，一个实例可以同时支持联机交易和实时分析等多种负载，省去数据同步的时间延迟和同步链路的维护成本。

## 应用场景

### AI 应用

面向以智能客服、知识库、智能推荐为代表的 RAG 场景，SeekDB 支持了完整的 RAG Pipeline 解决方案。内置文档解析处理功能（确认下发不发），集成 Embedding/Rerank/大语言模型，支持向量/全文/标量混合搜索，在一个数据库示例内实现 Doc In Data Out。
面向 AI 辅助编程和 AI Agent 开发场景，SeekDB 提供了秒级初始化和启动能力，支持作为底层 Tool 快速被拉起提供服务。支持高频增删改和实时查询，避免数据库性能瓶颈引起 AI 开发效率降低的问题。提供全文索引功能，精准查询代码关键词。提供 MCP Server，无缝接入 AI 生态。

### MySQL 场景升级

SeekDB 继承了 OceanBase 单机存储引擎、执行引擎、事务引擎、高级查询优化器的完整能力，高度兼容 MySQL。小规格可用于物联网边缘设备数据采集、小型应用开发和实验教学等场景。中大规格可用于各行业 OLTP、HTAP 的业务场景。



