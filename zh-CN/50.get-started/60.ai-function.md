# SeekDB AI 函数服务快速上手

本教程将引导您快速上手 SeekDB AI 函数服务，深入了解其如何利用 AI 能力，帮助您理解实际应用，并在几分钟内体验 AI 原生数据库的强大功能。

## 概述

AI 函数通过 SQL 表达式，将 AI 模型能力直接集成到数据库内的数据处理中。它极大地简化了利用 AI 大模型进行数据提取、分析、总结和保存等操作，是当前数据库和数据仓库领域的重要新特性。SeekDB 通过 `DBMS_AI_SERVICE` 包提供 AI 模型和端点管理，并新增了几个内置 AI 函数表达式，并支持通过视图监控 AI 模型调用情况。在 SQL 中直接调用 AI 模型，无需编写额外代码，几分钟内体验 `AI_COMPLETE`、`AI_EMBED`、`AI_RERANK` 三个核心函数：

* `AI_EMBED`：通过调用嵌入模型（Embedding Model）将文本数据转换为向量数据。
* `AI_COMPLETE`：通过调用指定文本生成大模型处理提示词和数据信息，并解析处理结果。
* `AI_RERANK`：通过调用重排序模型（Rerank Model）根据提示词对文本进行相似度排序。

该特性可以应用于文本生成、文本转换、文本重排序等场景。

## 前提条件

连接数据库
权限
注册 AI 函数服务
验证模型是否可用

## 步骤一：使用 AI_EMBED 生成向量

将文本转换为向量，用于向量检索

说明+示例

## 步骤二：使用 AI_COMPLETE 生成文本

在 SQL 中直接生成文本内容
AI_PROMPT 参数

说明+示例

## 步骤三：使用 AI_RERANK 优化检索结果

对检索结果进行智能重排序

说明+示例

## 步骤四：综合应用

三个函数组合使用

## 参数调优

看情况

## 总结