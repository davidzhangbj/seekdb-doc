---
slug: /openai-embedding-function-of-seekdb-js
---

# OpenAI 向量函数

使用 OpenAI 的向量 API。

:::tip

`OpenAIEmbeddingFunction` 目前为实验性功能。

:::

在首次使用时需要先安装，命令如下：

```shell
npm install @seekdb/openai
```

在项目中实例化并使用模型:

```js
import { OpenAIEmbeddingFunction } from "@seekdb/openai";

const openaiEmbed = new OpenAIEmbeddingFunction({
  // 您的 OpenAI API Key
  apiKey: "sk-...",
  // 可选,默认为 text-embedding-ada-002
  modelName: "text-embedding-ada-002"
});

const collection = await client.createCollection({
  name: "openai_embed_collection",
  embeddingFunction: openaiEmbed
});
```

## 相关操作

如果您需要使用其他内置函数或者自定义函数，可以参考以下文档创建和使用自定义函数：

* [默认嵌入式函数](100.default-embedding-function-of-seekdb-js.md)
* [Qwen 向量函数](200.qwen-embedding-function-of-seekdb-js.md)
* [Jina 向量函数](400.jina-embedding-function-of-seekdb-js.md)
* [创建自定义嵌入函数](500.create-custim-embedding-functions-of-seekdb-js.md)
* [使用自定义嵌入函数](600.using-custom-embedding-functions-of-seekdb-js.md)