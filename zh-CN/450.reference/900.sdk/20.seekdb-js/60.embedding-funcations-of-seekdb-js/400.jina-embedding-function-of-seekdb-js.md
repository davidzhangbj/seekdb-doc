---
slug: /jina-embedding-function-of-seekdb-js
---

# Jina 向量函数

使用 Jina AI 的向量服务。

在首次使用时需要先安装，命令如下：

```shell
npm install @seekdb/jina
```

在项目中实例化并使用模型:

```js
import { JinaEmbeddingFunction } from "@seekdb/jina";

const jinaEmbed = new JinaEmbeddingFunction({
  // 您的 Jina API Key
  apiKey: "jina_...",
  // 可选,默认为 jina-embeddings-v2-base-en
  modelName: "jina-embeddings-v2-base-en"
});

const collection = await client.createCollection({
  name: "jina_embed_collection",
  embeddingFunction: jinaEmbed
});
```

## 相关操作

如果您需要使用其他内置函数或者自定义函数，可以参考以下文档创建和使用自定义函数：

* [默认嵌入式函数](100.default-embedding-function-of-seekdb-js.md)
* [Qwen 向量函数](200.qwen-embedding-function-of-seekdb-js.md)
* [OpenAI 向量函数](300.openai-embedding-function-of-seekdb-js.md)
* [创建自定义嵌入函数](500.create-custim-embedding-functions-of-seekdb-js.md)
* [使用自定义嵌入函数](600.using-custom-embedding-functions-of-seekdb-js.md)